<dependencies>
    <!-- Dependency cho JavaFX Controls -->
    <dependency>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-controls</artifactId>
        <version>21.0.2</version> <!-- Bạn có thể dùng phiên bản mới hơn nếu muốn -->
    </dependency>

    <!-- Dependency cho JavaFX FXML (vì bạn đang dùng FXML) -->
    <dependency>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-fxml</artifactId>
        <version>21.0.2</version>
    </dependency>
</dependencies>